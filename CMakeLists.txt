CMAKE_MINIMUM_REQUIRED(VERSION 3.12)
PROJECT(POUQ)

SET(CMAKE_CXX_STANDARD 17)

FIND_PACKAGE(OpenMP REQUIRED)

if (ENABLE_TEST)
    ADD_EXECUTABLE(compare_error test/compare_error.cpp)

    TARGET_LINK_LIBRARIES(compare_error PRIVATE
            OpenMP::OpenMP_CXX
    )
endif ()